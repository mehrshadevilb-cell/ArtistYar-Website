import {NextResponse} from "next/server";import {cookies} from "next/headers";import {ADMIN_SESSION_COOKIE,verifyAdminSession} from "@/lib/server-admin-auth";import {validateCourse} from "@/lib/education-catalog";
export async function GET(req:Request){if(!verifyAdminSession((await cookies()).get(ADMIN_SESSION_COOKIE)?.value))return NextResponse.json({error:"admin_session_required"},{status:401});const id=Number(new URL(req.url).searchParams.get("courseId"));if(!Number.isInteger(id)||id<=0)return NextResponse.json({error:"invalid_course"},{status:400});try{return NextResponse.json(await validateCourse(id),{headers:{"Cache-Control":"private,no-store"}})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"validation_failed"},{status:500})}}
