import {NextResponse} from "next/server";import {cookies} from "next/headers";import {ADMIN_SESSION_COOKIE,verifyAdminSession} from "@/lib/server-admin-auth";import {listCanonicalCourses} from "@/lib/education-catalog";
export async function GET(){if(!verifyAdminSession((await cookies()).get(ADMIN_SESSION_COOKIE)?.value))return NextResponse.json({error:"admin_session_required"},{status:401});try{return NextResponse.json(await listCanonicalCourses(),{headers:{"Cache-Control":"private,no-store"}})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"course_catalog_unavailable"},{status:500})}}
