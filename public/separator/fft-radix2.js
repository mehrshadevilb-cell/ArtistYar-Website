const _twiddleCache = new Map();
function getTwiddles(n){let e=_twiddleCache.get(n);if(e)return e;const wr=new Float64Array(n),wi=new Float64Array(n);let off=0;for(let size=2;size<=n;size<<=1){const half=size>>1,step=-2*Math.PI/size;for(let k=0;k<half;k++){wr[off+k]=Math.cos(step*k);wi[off+k]=Math.sin(step*k)}off+=half}e={wr,wi};_twiddleCache.set(n,e);return e}
export function fftRadix2InPlace(buf,n){let j=0;for(let i=0;i<n;i++){if(i<j){let a=i<<1,b=j<<1,t=buf[a];buf[a]=buf[b];buf[b]=t;t=buf[a+1];buf[a+1]=buf[b+1];buf[b+1]=t}let m=n>>1;while(m>=1&&j>=m){j-=m;m>>=1}j+=m}const{wr,wi}=getTwiddles(n);let off=0;for(let size=2;size<=n;size<<=1){const half=size>>1;for(let i=0;i<n;i+=size)for(let k=0;k<half;k++){const wrk=wr[off+k],wik=wi[off+k],a=(i+k)<<1,b=(i+k+half)<<1,tr=buf[b]*wrk-buf[b+1]*wik,ti=buf[b]*wik+buf[b+1]*wrk;buf[b]=buf[a]-tr;buf[b+1]=buf[a+1]-ti;buf[a]+=tr;buf[a+1]+=ti}off+=half}}